# Copy to `.env` in this directory (not committed).
# Compose interpolates these into the `api` service.
# Local `npm run dev` still uses backend/.env.

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
# CLOUDINARY_URL=
CLOUDINARY_FOLDER=ecommercex/products
